import com.mathsystem.graphapi.AbstractGraph;
import com.mathsystem.graphapi.GraphFactory;

import java.io.File;

public class MainClass {
    public static void main(String[] args) {
        // Для теста написанного модуля, напишите следующий код
        // Вместо ClassName и className написать название класса с написанным модулем
        /*
        ClassName className = new ClassName();
        AbstractGraph abstractGraph = GraphFactory.loadDirectedGraphFromFile(
               new File("ПУТЬ ДО ПАПКИ С ГРАФОМ", "НАЗВАНИЕ ФАЙЛА С ГРАФОМ"));
        System.out.println(className.execute(abstractGraph));
        */
    }
}
